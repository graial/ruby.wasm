**This fork is the ongoing development of an IC deployable ruby build and discussion around what, if any, belongs in the upstream branch. Active development is on the `release` branch**

The current latest commit hash to pin your submodule to is `graial/ruby.wasm commit 18560d5`

The working demo repo for building this release bundle is located at: https://gitlab.com/phrug/ruby_wasm_builder


git clone --recurse-submodules https://gitlab.com/phrug/ruby_wasm_builder

More instructions to follow!

